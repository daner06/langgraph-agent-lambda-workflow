# LangGraph agent — monorepo

| Directory | Contents |
|-----------|----------|
| **`backend/`** | Python LangGraph agent, Lambda handler, `Dockerfile`, `deploy.sh`, Terraform (`backend/terraform/`) |
| **`frontend/`** | Reserved for a React app (empty placeholder; add your app here) |

- **Local dev & AWS deployment:** [backend/README.md](backend/README.md)
